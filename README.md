# Heart-disease-detection

STEP 1:
    
    DATABASE_URL = "postgresql://postgres:password@localhost/database_name" FROM API.PY
    Replace the Password and database_name with your database information 
STEP 2:
   RUN THE API.PY FORM ONE TERMINAL USE COMMAND:   uvicorn api:app --reload

STEP 3:
  NOW RUN THE FRONTEND.PY FROM ANOTHER TERMINAL USE COMMAND: streamlit run frontend.py
          
